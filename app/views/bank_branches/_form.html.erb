<%= form_for(@bank_branch) do |f| %>
  <% if @bank_branch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank_branch.errors.count, "error") %> prohibited this bank_branch from being saved:</h2>

      <ul>
      <% @bank_branch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
