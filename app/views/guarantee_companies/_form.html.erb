<%= form_for(@guarantee_company) do |f| %>
  <% if @guarantee_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guarantee_company.errors.count, "error") %> prohibited this guarantee_company from being saved:</h2>

      <ul>
      <% @guarantee_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
