<%= form_for(@bank_card_type) do |f| %>
  <% if @bank_card_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank_card_type.errors.count, "error") %> prohibited this bank_card_type from being saved:</h2>

      <ul>
      <% @bank_card_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
